<?php
/**
 * EU VAT module path anchor for the merged VibeShift EU Shipping plugin.
 *
 * Not a standalone WordPress plugin header. Loaded only so
 * plugin_dir_path( WC_EU_VAT_FILE ) resolves to this module root
 * (templates/, build/, languages/).
 *
 * @package vibeshift-eu-shipping
 */

// Intentionally empty path anchor.
